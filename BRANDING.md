# Branding Reference

Current brand name: **Agent Claw**

This document maps every location in the codebase where the brand name appears. Use it as a checklist when rebranding.

---

## 1. User-Facing Text (UI strings, page titles)

These are what end users see. Change these first during a rebrand.

| File | Line(s) | What | Current Value |
|------|---------|------|---------------|
| `app/layout.tsx` | ~10 | `<title>` metadata | `"Agent Claw -- Command Centre"` |
| `components/Sidebar.tsx` | ~84 | Sidebar header fallback name | `'Agent Claw'` |
| `components/MobileSidebar.tsx` | ~138, ~226 | Mobile sidebar fallback name | `'Agent Claw'` |
| `components/OnboardingWizard.tsx` | ~271 | Welcome screen heading | `"Welcome to Agent Claw"` |
| `components/OnboardingWizard.tsx` | ~282 | Welcome screen description | `"A visual command centre for your AI agent team..."` |
| `components/OnboardingWizard.tsx` | ~572 | Name input placeholder | `"Agent Claw"` |
| `components/OnboardingWizard.tsx` | ~675 | Sidebar preview fallback | `'Agent Claw'` |
| `components/GlobalSearch.tsx` | ~349, ~389-390 | Search modal aria-label + placeholder | `"Search Agent Claw"` / `"Search Agent Claw..."` |
| `app/chat/page.tsx` | ~173 | Chat page header | `"Agent Claw Messages"` |
| `lib/agents.json` | ~5, ~14 | Default root agent title + description | `"Agent Claw Orchestrator"` |
| `scripts/setup.mjs` | ~77 | Setup script banner | `"Agent Claw Setup"` |
| `scripts/setup.mjs` | ~184 | Generated .env.local comment | `"# Agent Claw -- generated by npm run setup"` |
| `scripts/setup.mjs` | ~205 | Post-setup instructions | `"Start Agent Claw"` |
| `.env.example` | ~2 | File header comment | `"# Agent Claw -- Environment Configuration"` |

## 2. Internal Code Identifiers

TypeScript interfaces, function names, component names, and variable names. These are developer-facing only. Renaming requires updating all imports and references.

| Identifier | Files | Notes |
|------------|-------|-------|
| `ManorSettings` (interface) | `lib/settings.ts`, `app/settings-provider.tsx`, `docs/THEMING.md`, `docs/COMPONENTS.md` | Core settings type. 20+ references. |
| `manorName` (field) | `lib/settings.ts`, `app/settings-provider.tsx`, `components/Sidebar.tsx`, `MobileSidebar.tsx`, `OnboardingWizard.tsx`, `app/settings/page.tsx`, `DynamicFavicon.tsx` | Settings field for custom name. |
| `manorSubtitle` (field) | Same as `manorName` | Settings field for subtitle. |
| `manorEmoji` (field) | Same as `manorName` | Settings field for emoji. |
| `manorIcon` (field) | Same as `manorName` | Settings field for uploaded icon image. |
| `setManorName` (setter) | `app/settings-provider.tsx`, `OnboardingWizard.tsx`, `app/settings/page.tsx` | Context setter callback. |
| `setManorSubtitle` (setter) | Same as `setManorName` | |
| `setManorEmoji` (setter) | Same as `setManorName` | |
| `setManorIcon` (setter) | Same as `setManorName` | |
| `ManorMap` (component) | `components/ManorMap.tsx`, `app/page.tsx`, `docs/COMPONENTS.md` | React Flow org chart component. |
| `ManorMapProps` (interface) | `components/ManorMap.tsx` | Props type for ManorMap. |
| `ManorPage` (component) | `app/page.tsx` | Home page component. |
| `handleManorIconUpload` (function) | `app/settings/page.tsx` | Icon upload handler. |
| `manorIconInputRef` (ref) | `app/settings/page.tsx` | File input ref. |

## 3. localStorage Keys

Changing these breaks existing users' saved data. Requires a migration function or accept data loss.

| Key | File(s) | Purpose |
|-----|---------|---------|
| `manor-settings` | `lib/settings.ts` | All user settings (name, subtitle, emoji, icon, accent, etc.) |
| `manor-theme` | `app/providers.tsx` | Selected theme ID |
| `manor-onboarded` | `components/OnboardingWizard.tsx` | First-run completion flag |
| `manor-conversations` | `lib/conversations.ts` | Chat message history per agent |
| `manor-kanban` | `lib/kanban/store.ts` | Kanban board state |

## 4. Custom Events

| Event Name | File(s) | Purpose |
|------------|---------|---------|
| `manor:open-search` | `components/Sidebar.tsx`, `components/GlobalSearch.tsx` | Triggers Cmd+K search modal |

## 5. API / Session Keys

| Key | File | Purpose |
|-----|------|---------|
| `agent:main:manor-ui` | `lib/anthropic.ts` | Default session key for OpenClaw gateway calls |
| `manor-${Date.now()}-...` | `lib/anthropic.ts` | Idempotency key prefix for chat.send |

## 6. Package / Repository

| Location | Current Value |
|----------|---------------|
| `package.json` `name` | `manor-ui` |
| `package-lock.json` `name` | `manor-ui` |
| Git clone URLs in docs | `https://github.com/openclaw/manor-ui.git` |

## 7. Workspace Paths

The user's agent registry override lives at `$WORKSPACE_PATH/manor/agents.json`. This path is referenced in:

| File | What |
|------|------|
| `lib/agents-registry.ts` | `join(workspacePath, 'manor', 'agents.json')` |
| Multiple test files | Mock paths like `/tmp/test-workspace/manor/agents.json` |
| Documentation | Setup instructions referencing `$WORKSPACE_PATH/manor/` |

## 8. Documentation Files

Every `.md` file contains brand references. Full list:

| File | Approximate Occurrences |
|------|------------------------|
| `README.md` | ~20 |
| `SETUP.md` | ~25 |
| `CLAUDE.md` | ~20 |
| `docs/API.md` | ~5 |
| `docs/THEMING.md` | ~30 |
| `docs/COMPONENTS.md` | ~35 |
| `.env.example` | 1 |

## 9. Test Files

Tests reference brand strings in mock data and localStorage keys:

| File | What |
|------|------|
| `lib/settings.test.ts` | `manor-settings` key, `manorName`/`manorSubtitle`/`manorEmoji`/`manorIcon` fields |
| `lib/agents.test.ts` | `manor/agents.json` paths, "Manor Orchestrator" in mock data |
| `lib/conversations.test.ts` | `manor-conversations` key |
| `lib/kanban/store.test.ts` | `manor-kanban` key |

---

## Rebrand Checklist

1. **User-facing text** (Section 1) -- find-and-replace the display name
2. **Documentation** (Section 8) -- update all .md files
3. **Package name** (Section 6) -- update package.json, re-run npm install
4. **Internal identifiers** (Section 2) -- rename interfaces, components, functions, fields
5. **localStorage keys** (Section 3) -- add migration in `loadSettings()` to read old keys
6. **Custom events** (Section 4) -- rename event strings
7. **API keys** (Section 5) -- update session key and idempotency prefix
8. **Workspace paths** (Section 7) -- update `agents-registry.ts` path, support both old/new
9. **Test files** (Section 9) -- update all mock data and key references
